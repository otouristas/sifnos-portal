# Netlify redirects file
# Handle client-side routing for React SPA

# Ensure static files are served correctly (don't redirect these)
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200
/llm.txt /llm.txt 200
/manifest.json /manifest.json 200
/favicon.ico /favicon.ico 200
/touristas-ai-logo.svg /touristas-ai-logo.svg 200
/*.svg /:splat.svg 200
/*.xml /:splat.xml 200
/*.txt /:splat.txt 200
/*.json /:splat.json 200
/*.ico /:splat.ico 200
/*.png /:splat.png 200
/*.jpg /:splat.jpg 200
/*.webp /:splat.webp 200

# API routes (if any)
/api/* /api/:splat 200

# Handle all routes for client-side routing (must be last)
/* /index.html 200
